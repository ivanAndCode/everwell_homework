{
  "name": "everwell_homework",
  "version": "1.0.0",
  "description": "Take home task for the technical interview",
  "scripts": {
    "cy:run:all": "cypress run -s '**'",
    "cy:run:business:flows": "cypress run -s '**/ui/flows/*.js'",
    "cy:run:features": "cypress run -s '**/ui/features/*.js'",
    "cy:run:api": "cypress run -s '**/api/*'",
    "cy:open": "cypress open"
  },
  "author": "Ivan Karaman (aka ivanandcode)",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.13.2",
    "@testing-library/cypress": "^10.0.2"
  }
}
